=== ЛЁНЯ (SEO-АУДИТ) ===
## Аудит Лёни
Статус: ❌ НУЖНА ПЕРЕСБОРКА
URL: https://advokat-vsem.online/otvet-na-pretensiyu-po-intellektualnoj-sobstvennosti/
Артефакты:
- Markdown: ручной аудит (IndexLift `run-audit.js` в среде недоступен)
- JSON: curl + WebFetch HTML-снимок страницы, Yoast REST `/wp-json/wp/v2/pages/364`

### Проверка по критериям

| Критерий | Статус | Детали |
|----------|--------|--------|
| Title | ⚠️ | `Ответ на претензию по интеллектуальной собственности: сроки и типовые ошибки - Legis 24` — **87 симв.** (рекомендация Коли: 49). Релевантен теме, но обрезается в выдаче. |
| Description | ❌ | `<meta name="description">` **отсутствует** в HTML. Yoast REST: `description: null`, при этом `og:description` и `post_excerpt` заполнены (202 симв.). Сниппет Google/Яндекс не получит description. |
| H1 | ❌ | **2× H1 на странице**: Divi `entry-title main_title` + hero `hero-ip-pret-otvet__h1`. Текст совпадает с ТЗ Коли — дубль тега, не текста. |
| Структура H2 | ✅ | 10 H2, полное совпадение с ядром Коли: что такое претензия → сроки → как ответить → ошибки → доказательства → последствия → юрист → FAQ → Итог. H3 по ТЗ/авторским правам на месте. |
| Внутренние ссылки / CTA | ⚠️ | **5 внешних CTA** на `advokat-vsem.ru` с релевантными анкорами (консультация, разбор претензии). Якорное оглавление `#ip-pret-*` — ок. **Кontекстных внутренних ссылок в теле статьи нет**; IP-кластер (`narushenie-tovarnogo-znaka…`, `zashchita-tovarnogo-znaka…`) только в виджете/футере, не в лонгриде. |
| GEO (FAQ) | ✅ | Блок `#ip-pret-faq` с `itemtype="FAQPage"`, 5 Q&A (ст. 1252, срок «10 дней», ответ по ТЗ, comp 214-ФЗ, последствия молчания). Покрывает PAA/AI Overview. |
| Релевантность IP-теме | ✅ | Контент закрывает интент «ответ на претензию по ИС»: ст. 1252/1486, 214-ФЗ, ТЗ vs авторское, comp до 10 млн ₽, практика ВС/СИП. Тема не уходит в налоги/регистрацию. |

### Что исправить Юре
1. **Заполнить meta description в Yoast** (или вывести `post_excerpt` в `<meta name="description">` через шаблон `page-{slug}.php`). Сейчас excerpt есть в WP, но тег в `<head>` не рендерится — `yoast_head_json.description = null`.
2. **Убрать дубль H1**: скрыть/удалить Divi `entry-title main_title` на кастомном шаблоне или заменить hero `<h1>` на `<p>` с классом (оставить один H1 на странице).
3. **Сократить Title** до варианта Коли: `Ответ на претензию по ИС: сроки и типовые ошибки` (+ бренд при необходимости, ≤60 симв. до суффикса).
4. **Добавить 2–3 контекстные внутренние ссылки** в лонгрид на IP-страницы кластера: `/narushenie-tovarnogo-znaka-dokazatelstva-kompensaciya/`, `/zashchita-tovarnogo-znaka-isk-po-intellektualnoj-sobstvennosti/` (в секциях про доказательства/последствия).

### Приоритет
- Critical: meta description отсутствует; дубль H1
- High: нет in-content внутренних ссылок на IP-кластер
- Medium: Title длиннее рекомендованного (87 vs 49–60 симв.)

### Итог
Страница по содержанию и H2/FAQ готова к GEO и IP-теме, но **не готова к индексации без пересборки**: отсутствует `<meta name="description">` и два H1 — критичные дефекты для Google и Яндекса.
